{
    "private": true,
    "scripts": {
        "dev": "firebase use stage && firebase serve",
        "host-prod": "firebase use prod && firebase deploy",
        "host-stage": "firebase use stage && firebase deploy"
    }
}
